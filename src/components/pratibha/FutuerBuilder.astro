---
import { getEntry } from "astro:content";
const futurebuilder = await getEntry("pratibha", "future-builder");

const { title, paragraphs, button, imageGrid } = futurebuilder?.data ?? {};
const { mt } = Astro.props;
---

<!-- <section class={`max-w-[1296px] mx-auto px-4 md:px-8 py-24 ${mt}`}> --><!-- Heading + Description with Optional Top Image --><!-- <div class="grid md:grid-cols-2 gap-12 items-center"
  data-aos="fade-left"
        data-aos-duration="1200"
        data-aos-offset="120px"
        data-disable-parallax-down="lg">
    <div class="space-y-[1.5rem] font-poppins"> -->
<section
  class=`   justify-center lg:max-w-[79.5rem] mx-auto pr-[1.5rem] pl-[1.5rem]  ${mt}   md:gap-16 grid tb:grid-cols-1 md:grid-cols-2 gap-16  items-center `
>
  <div
    class="md:max-w-[39.75rem] mx-auto tb:items-center space-y-[1.5rem]"
  
  >
    <h1
      class="pratibha-subheading"
    >
      {title}
    </h1>

    {
      paragraphs &&
        paragraphs.map((par) => (
          <p class="pratibha-description text-[#49494C]"
          data-aos="fade-right"
    data-aos-duration="1200"
    data-disable-parallax-down="lg"
          >
            {par}
          </p>
        ))
    }
    <div
      class="flex flex-col sm:flex-row mt-[1.5rem] lg:max-w-[13.0625rem] "
      data-aos="fade-left"
      data-aos-duration="1200"
      data-aos-offset="120px"
      data-disable-parallax-down="lg"
    >
      <a
        href={button.herf}
        class="inline-flex font-poppins items-center justify-center px-3 py-[.75rem] bg-[#00AA5E] text-black rounded-md hover:bg-[#009b55] transition-colors font-semibold text-md"
      >
        {button.text}
      </a>
    </div>

  </div>
  <div class="image-team-grid w-full md:max-w-[37.25rem] mx-auto" >
    <div
      class="grid md:grid-cols-[65%_25%] grid-rows-[auto_1fr] gap-4 w-full h-full"
    >
      <div class="team-top-image col-span-2 w-full h-full">
        <img
          src={imageGrid.image}
          alt=""
          class="rounded-xl w-full h-full object-cover kiran-image max-h-[12.5rem] aspect-video shadow-xl"
        />
      </div>

      <div class="bottom-left w-full h-full">
        <img
          src={imageGrid.image2}
          alt=""
          class="w-full h-full object-cover aspect-video rounded-xl
             kiran-image-small tb:max:w-[20%] shadow-xl"
        />
      </div>

      <div class="bottom-right w-full h-full">
        <img
          src={imageGrid.image2}
          alt=""
          class="rounded-xl w-full h-full object-cover"
        />
      </div>
    </div>
  </div>
  <!-- Image beside title + description, if `imageTop` exists -->
  <!-- {imageGrid && (
      <div class="w-full h-64 md:h-80 rounded-lg overflow-hidden">
        <img src={imageGrid.image} alt={title} class="w-full h-full object-cover" />
      </div>
    )} -->
</section>

<!-- Dynamic Image (swappable) -->
<!-- </section> -->
<!-- 
<PratibhaLayout mt={`${mt} gap-16`}>
  <Fragment slot="content">
    <div class="fq flex flex-col md:max-w-[39.75rem]">
      <div
        class="md:max-w-[39.75rem] space-y-[1.5rem] max-w-[45.75rem] font-poppins"
      >
        <h2 class="text-[2rem]">{title}</h2>

        {paragraphs && paragraphs.map((par) => <p>{par}</p>)}

        <div class="flex flex-col sm:flex-row mt-[1.5rem]">
          <a
            href={button.herf}
            class="inline-flex font-poppins items-center justify-center px-3 py-[.75rem] bg-[#00AA5E] text-black rounded-md hover:bg-[#009b55] transition-colors font-semibold text-md"
          >
            {button.text}
          </a>
        </div>
  
  </Fragment>

  <Fragment slot="image-grid">
    <div
      class="kiran-images max-w-[37.25rem] tb:max-h-[32.75rem] flex flex-1 flex-col gap-4"
    >
      <div class="image-top max-w-full">
        <img
          src={imageGrid.image}
          alt="Students from Kiran Foundation"
          class="kiran-image w-full max-h-[12.5rem] object-cover rounded-xl aspect-video shadow-xl"
        />
      </div>
      <div class="image-bottom flex gap-4 w-full">
        <div class="image-bottom-left flex-2">
          <img
            src={imageGrid.image2}
            alt="More students from Kiran Foundation"
            class="kiran-image-small rounded-xl tb:max:w-[60%] h-full object-cover shadow-xl"
          />
        </div>
        <div class="image-bottom-right flex-1">
          <img
            src={imageGrid.image2}
            alt="More students from Kiran Foundation"
            class="kiran-image-small rounded-xl tb:max:w-[20%] h-full object-cover shadow-xl"
          />
        </div>
      </div>
    </div>
  </Fragment>
</PratibhaLayout>

<style>

  h2 {
    font-family: Poppins;
    font-weight: 600;
    font-size: 2rem;
    line-height: 100%;
    letter-spacing: 0%;
  }
  p {
    font-family: Poppins;
    font-weight: 400;
    font-size: 1.125rem;
    line-height: 2.25rem;
    letter-spacing: 0%;
  }
  .button {
    width: 10.625rem;
    height: 3.4375rem;
    border-radius: 8px;


    background-color: #00aa5e;
  }
  @media (max-width: 1024px) {
    .fq {
      flex-direction: column;
    }
    .kiran-images {
      width: 100%;
    }
    .ds {
      max-width: 39.5rem;
    }
  }
</style> -->
