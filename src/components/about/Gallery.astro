---
import { getEntry } from "astro:content";
import GalleryImage from "./GalleryImage.astro"
const {sections} = (await getEntry("about", "gallery")).data;
---

{
  sections.map((section) => (<GalleryImage section={section} />))
}
